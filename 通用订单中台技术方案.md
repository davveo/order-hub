# 通用订单中台（Order Hub）技术方案

> 状态：草案 v0.1  
> 定位：面向任意交易场景的「订单生命周期中台」——管下单、金额快照、支付中、履约、取消、售后；**编排**用户中心、优惠中心、账本中台，**不替代**它们。  
> 对接：Auth Hub（`user_id` / Token）、OfferHub（试算—锁定—核销—撤销）、Ledger Hub（Freeze / Capture / Release / Credit）。  
> 架构图：[order-hub-architecture.html](./order-hub-architecture.html)（中台平面 / Checkout Saga / 状态机）

---

## 1. 背景与目标

### 1.1 问题

用户中心已经回答「是谁」，优惠中心已经回答「这单能减多少」，账本已经回答「有多少钱」。缺的是把这三件事串成一笔生意：

- 各业务自己写下单接口，状态机、超时关单、幂等各搞一套
- 优惠试算了却不下单锁定，或下单后不核销，券和预算对不上
- 积分/余额在订单里直接 `points--`，绕过账本，无法对账
- 电商、会员、课程、积分商城各做一套「订单表」，无法复用

### 1.2 建设目标

| 目标 | 说明 |
|------|------|
| 通用订单 | 用 `scene` + 行对象快照承载商城、会员、课程、积分兑换、SaaS 订阅等 |
| 编排而非吞并 | 订单只编排；身份、优惠、记账、支付通道、库存各归原系统 |
| 金额可信 | 最小货币单位整数；应付金额以订单落库快照为准 |
| 状态可恢复 | 超时关单、支付回调、部分退都能补偿到优惠和账本 |
| 幂等可对账 | `client_order_id` + `biz_no` 贯穿优惠锁定与账本流水 |
| 快速接入 | 新场景注册 scene 配置 + 履约适配器，不改订单内核 |

### 1.3 非目标（明确不做）

- 登录、账号绑定、Token 签发（用户中心 / Auth Hub）
- 活动规则、券生命周期、优惠计算（OfferHub）
- 余额加减、冻结内核（Ledger Hub）
- 支付渠道路由、清分、渠道对账主体（支付中台；本方案只对接适配器）
- SPU/SKU 主数据、仓配批次（商品 / 库存中心；订单只存下单快照）
- 实时动态定价中心（价格以调用方快照为准）

### 1.4 一句话定位

> **Order Hub = 交易世界的「单据与编排内核」：管订单状态和金额快照，把用户、优惠、账本、支付、履约按 Saga 串起来。**

中台地图：

```text
用户中心  管「是谁」     →  user_id / tenant_id / 会员属性快照
优惠中心  管「减多少」   →  quote → reserve → commit / release / reverse
账本中台  管「有多少」   →  Freeze → Capture / Release ；退款 Credit
订单中台  管「这笔单」   →  状态机 + 金额快照 + 编排（本方案）
支付中台  管「怎么收」   →  渠道意图；结果回告订单
履约适配  管「怎么给」   →  发货 / 开通权益 / 发虚拟物
```

---

## 2. 核心概念

```text
Tenant
  └── Scene（mall_checkout / membership / course / point_mall / saas …）
        └── Order
              ├── Buyer          ← Auth Hub user_id
              ├── OrderLine[]    ← 对象快照（sku / service / sku_point / entitlement）
              ├── Amounts        ← original / discount / payable / paid / refunded
              ├── PromotionRef   ← quote_id / reservation_id / redemption_id
              ├── LedgerRef      ← freeze_id / asset_code / biz_no
              ├── PaymentRef     ← payment_intent_id / channel
              └── FulfillmentRef ← adapter 回写
```

| 概念 | 定义 |
|------|------|
| **Order** | 一笔交易单据，主键 `order_id`，对外稳定键 `client_order_id` |
| **Scene** | 场景配置：要不要优惠、要不要账本预占、超时、是否支付即完成 |
| **OrderLine** | 行。`line_id` 在单内唯一，与 OfferHub 分摊、退款定位对齐 |
| **AmountSnapshot** | 下单瞬间的金额与币种，后续规则变化不影响本单 |
| **PromotionRef** | 本单使用的优惠报价/锁定/核销引用，不存规则本身 |
| **LedgerRef** | 本单对应的账本冻结单；积分抵扣、余额支付走这里 |
| **PayMethod** | `channel` 第三方支付 / `ledger` 账本扣减 / `mix` 混合 |
| **FulfillmentAdapter** | 按 scene 插拔的履约：发货、开通会员、发放课程、发虚拟资产 |

金额恒等式（单币种）：

```text
original_amount = Σ (unit_price × quantity)   // 含运费行
payable_amount  = original_amount - discount_amount
payable_amount  = ledger_pay_amount + channel_pay_amount
paid_amount     ≤ payable_amount
refunded_amount ≤ paid_amount
```

> 与 OfferHub 约定一致：金额一律最小货币单位整数；**订单拥有最终应付金额决定权**。报价与快照不一致则拒单并要求重新试算。

---

## 3. 总体架构

```text
┌──────────┐  ┌──────────┐  ┌──────────┐
│ App/Web  │  │ 业务 BFF │  │ 运营后台 │
└────┬─────┘  └────┬─────┘  └────┬─────┘
     │ HTTPS + Bearer Token      │
     ▼                           ▼
┌─────────────────────────────────────────────┐
│              API Gateway                    │
│     验票 / 限流 / 租户 / Idempotency-Key     │
└─────────────────────┬───────────────────────┘
                      ▼
┌─────────────────────────────────────────────┐
│                 Order Hub                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────────┐ │
│  │ Preview  │ │ Checkout │ │ After-sale   │ │
│  │ 试算编排 │ │ 下单编排 │ │ 退款/关单    │ │
│  └──────────┘ └────┬─────┘ └──────────────┘ │
│  ┌──────────┐ ┌────┴─────┐ ┌──────────────┐ │
│  │ State    │ │ Amount   │ │ Outbox       │ │
│  │ 状态机   │ │ 金额引擎 │ │ 领域事件     │ │
│  └──────────┘ └──────────┘ └──────────────┘ │
└───┬────────────┬────────────┬───────────┬───┘
    │            │            │           │
    ▼            ▼            ▼           ▼
 Auth Hub    OfferHub     Ledger Hub   Payment /
 introspect  quote/       Freeze/      Fulfillment
             reserve/     Capture/     Adapter
             commit       Release
```

设计原则：

1. **内核只认单据**：不认「满减」「微信回调」「会员开通」等外部语义；语义进 scene 与适配器。
2. **同步拿结果，异步发事件**：锁定/冻结必须同步成功才进入待支付；下游通知走 Outbox。
3. **禁止分布式事务**：与优惠、账本、支付之间用 Saga + 补偿，不用 2PC。
4. **身份只信 Token**：`user_id` / `tenant_id` 来自用户中心 introspect，不信请求体。

---

## 4. 场景配置（通用性的真正来源）

订单内核相同；差异全部配置化。

```yaml
scenes:
  mall_checkout:
    currency: CNY
    offer: required_or_skip      # 可无券下单
    ledger: optional             # 可选积分抵扣
    inventory: reserve_on_create
    pay_timeout: 15m
    auto_complete_on_paid: false
    fulfillment: shipping

  point_mall:
    currency: POINT              # 应付资产即积分
    offer: optional
    ledger: freeze_capture       # 下单 Freeze，支付/确认 Capture
    pay_timeout: 15m
    auto_complete_on_paid: true
    fulfillment: virtual_grant

  membership:
    currency: CNY
    offer: optional
    ledger: optional
    pay_timeout: 30m
    auto_complete_on_paid: true
    fulfillment: entitlement

  course:
    currency: CNY
    offer: optional
    pay_timeout: 30m
    auto_complete_on_paid: true
    fulfillment: entitlement

  saas_subscription:
    currency: CNY
    offer: optional
    recurring: true              # 二期：续费单
    fulfillment: entitlement
```

新增业务：加一条 scene + 实现 `FulfillmentAdapter`，不必新做订单系统。

---

## 5. 订单状态机

内核状态（全 scene 共用）。个别 scene 可跳过 `FULFILLING`（支付成功直接 `COMPLETED`）。

```text
                ┌──────────────┐
     创建成功 → │ PENDING_PAY  │
                └──────┬───────┘
           支付成功 / 账本 Capture
                       ▼
                ┌──────────────┐     需履约
                │     PAID     │──────────────┐
                └──────┬───────┘              ▼
         auto_complete │               ┌────────────┐
                       │               │ FULFILLING │
                       ▼               └─────┬──────┘
                ┌──────────────┐             │ 履约完成
                │  COMPLETED   │◄────────────┘
                └──────┬───────┘
                       │ 售后
                       ▼
                ┌──────────────┐
                │  REFUNDING   │──成功──► PARTIAL_REFUNDED / REFUNDED
                └──────────────┘

PENDING_PAY ──用户取消 / 超时──► CANCELLED
PAID / FULFILLING 允许关闭的 scene ──► CLOSED（先补偿再关）
```

| 状态 | 含义 | 优惠 | 账本 |
|------|------|------|------|
| `PENDING_PAY` | 单已落、资源已锁、等待支付 | Reservation ACTIVE | Freeze 已占用 |
| `PAID` | 钱已确认 | 已 commit | 已 Capture（若有） |
| `FULFILLING` | 履约中 | 已核销 | 已落定 |
| `COMPLETED` | 完成 | 已核销 | 已落定 |
| `CANCELLED` | 未支付关闭 | release | Release |
| `REFUNDING` | 售后处理中 | 按策略 reverse | Credit / 冲正 |
| `REFUNDED` / `PARTIAL_REFUNDED` | 退款完成 | 已撤销（或部分） | 已加回 |

迁移必须条件更新：`UPDATE ... WHERE status=? AND version=?`。禁止应用内存里改完直接覆盖。

---

## 6. 与三套已有系统的对接契约

### 6.1 用户中心（Auth Hub）

| 事项 | 约定 |
|------|------|
| 身份主键 | 只使用 `user_id`，与 identities（手机/邮箱/OAuth）解耦 |
| 鉴权 | Gateway 或订单服务调用 `POST /api/v1/auth/introspect`；高并发可 JWKS 本地验签 |
| 租户 | Token / 应用凭证中的 `tenant_id`，写入订单且后续调用优惠/账本原样透传 |
| 下单主体 | `buyer.holder_type=user`，`buyer.user_id` 取自 Token，**忽略 body 里的 user_id** |
| 会员属性 | 优惠中心不反查用户库。订单在 Preview/Checkout 时组装 `subject.attributes`（等级、新客、标签）。属性来源可以是用户中心扩展字段、权益中心或 BFF 传入的已治理字段 |
| 应用身份 | 订单服务使用自己的 `client_id` 调优惠/账本，与用户 Token 分离 |

组装给 OfferHub 的主体：

```json
{
  "type": "user",
  "id": "u_123",
  "attributes": {
    "member_level": "gold",
    "is_new_user": false
  }
}
```

字段必须落在 OfferHub 字段注册表内；订单不传未注册的敏感 PII。

### 6.2 优惠中心（OfferHub）

完整生命周期对齐 OfferHub 已实现协议，前缀 `/api/discount/v1`：

```text
available → quotes → reservations → commit | release
已核销售后 → redemptions/{id}/reverse
长支付 → reservations/{id}/renew
```

| 订单动作 | OfferHub 调用 | 幂等键建议 |
|----------|----------------|------------|
| Preview | `POST /quotes`（可先 `/available`） | 只读，可不带 |
| Checkout 成功落单后 | `POST /reservations` | `order:{order_id}:reserve` |
| 支付成功 | `POST /reservations/{id}/commit` | `order:{order_id}:commit` |
| 取消 / 超时 | `POST /reservations/{id}/release` | `order:{order_id}:release` |
| 支付中即将过期 | `POST /reservations/{id}/renew` | `order:{order_id}:renew:{n}` |
| 退款 | `POST /redemptions/{id}/reverse` | `order:{refund_id}:reverse` |

下单时传入稳定 `business_order_id = order_id`（OfferHub 第二重防重）。

交易上下文由订单生成，**禁止优惠中心回调订单库**：

- `transaction.id` = 预生成 `order_id`（Preview 可用 `preview:{client_order_id}`，Checkout 换成正式单号并**重新 quote**，或 Checkout 复用未过期且 `context_hash` 一致的 quote）
- `transaction.amount` = 各行金额之和，否则 OfferHub 拒算
- `items[].line_id` 与订单行一致，退款按行分摊

订单对报价的裁决：

1. 校验 `currency`、`original_amount` 与本地行合计一致  
2. 采纳 `discount_amount` / 行级 `allocations` 写入快照  
3. `payable_amount = original - discount`；若调用方想改应付价，必须重新 quote，禁止手工改折扣  
4. quote 过期或 `QUOTE_CHANGED` → 返回 `QUOTE_STALE`，让 C 端重试算

### 6.3 账本中台（Ledger Hub）

`source_system = order`。命令与订单状态对齐技术方案第 7 章：

| 订单事件 | 账本命令 | `biz_no` |
|----------|----------|----------|
| 积分/余额预占 | `Freeze` | `order:freeze:{order_id}` |
| 支付确认（含纯账本支付） | `Capture` | `order:capture:{order_id}` |
| 取消 / 超时 | `Release` | `order:release:{order_id}` |
| 售后退回资产 | `Credit` | `order:refund:{refund_id}` ，`related_biz_no=order:capture:{order_id}` |

混合支付：

```text
应付 228.00 CNY
  ├─ 优惠已减（OfferHub，不走账本）
  ├─ 积分抵扣 50 POINT     → Freeze POINT
  └─ 渠道支付剩余 CNY      → Payment Adapter
支付成功：Capture POINT + 标记渠道已付
```

纯积分商城：应付资产即 `POINT`，可无渠道，Freeze 后用户确认或自动 Capture。

账本余额不足（`42201`）→ 订单创建失败，**不得**先落 `PENDING_PAY` 再欠账。若优惠已 reserve，必须立刻 release。

### 6.4 支付与履约（适配器，非本系统内核）

```go
type PaymentAdapter interface {
    CreateIntent(ctx context.Context, o Order) (PaymentIntent, error)
    Refund(ctx context.Context, refund Refund) error
}

type FulfillmentAdapter interface {
    Reserve(ctx context.Context, o Order) error // 库存预占等，可空实现
    Commit(ctx context.Context, o Order) error  // 发货 / 开通权益
    Release(ctx context.Context, o Order) error
}
```

支付成功只接受：**渠道回调验签后的内部接口**，或账本 Capture 成功。客户端不能直接把订单改成 `PAID`。

---

## 7. 核心编排（Saga）

### 7.1 Preview（不算资源）

```text
Client → Order Hub
  1. introspect Token → user_id, tenant_id
  2. 组装交易快照（行、金额、渠道、会员属性）
  3. POST OfferHub /available 、 /quotes
  4. 可选：查询 Ledger 余额（只读）
  5. 返回 quote_id + payable + 分摊 + 过期时间
不落订单，不 reserve，不 Freeze。
```

### 7.2 Checkout（下单）

正向顺序（成功才进入 `PENDING_PAY`）：

```text
1. 验票 + 幂等（client_order_id / Idempotency-Key）
2. 校验 scene、行金额、币种
3. 若有 quote：校验未过期且 context_hash 一致；否则现场重 quote
4. 预生成 order_id
5. OfferHub reserve(quote_id, business_order_id=order_id)
6. 若需账本支付/抵扣：Ledger Freeze
7. 若 scene 需要库存：Fulfillment.Reserve
8. 本地事务插入 order / lines / amount_snapshot / refs（status=PENDING_PAY）
9. 写 Outbox：order.created.v1
10. 创建 PaymentIntent（渠道支付时）
```

任一步失败，**逆序补偿已成功步骤**：

```text
库存 Reserve 失败 → Ledger Release → OfferHub release → 不落单或标 FAILED
Freeze 失败       → OfferHub release
reserve 失败      → 直接返回错误
落库失败          → Release + release（补偿必须幂等，可重试）
```

补偿与正向一样带稳定幂等键；OfferHub / Ledger 均支持重复提交返回首次结果。

### 7.3 支付成功

```text
支付回调 / 纯账本确认
  → 条件更新 PENDING_PAY → PAID
  → OfferHub commit
  → Ledger Capture（若有 freeze）
  → Fulfillment.Commit
  → scene.auto_complete_on_paid ? COMPLETED : FULFILLING
  → Outbox: order.paid.v1
```

`commit` / `Capture` 失败：订单停在 `PAID` 或进入 `COMPENSATING`，由对账任务补单，**禁止**把已收款订单当取消。

### 7.4 取消与超时

```text
PENDING_PAY + (用户取消 | 超过 pay_timeout)
  → 条件更新 → CANCELLED
  → OfferHub release
  → Ledger Release
  → Fulfillment.Release
  → 关闭 PaymentIntent（若渠道支持）
  → Outbox: order.cancelled.v1
```

关单与支付回调并发：以条件更新赢家为准。若已 `PAID`，取消请求返回 `ORDER_ALREADY_PAID`。

### 7.5 售后

```text
1. 生成 refund_id，金额 ≤ 可退（按行分摊，沿用下单时 OfferHub allocations）
2. 渠道退款 和/或 Ledger Credit
3. OfferHub reverse（全额或部分；是否退券看活动 reversal_policy）
4. 更新 refunded_amount；全部退完 → REFUNDED，否则 PARTIAL_REFUNDED
```

退款**不重新跑当前优惠规则**，与 OfferHub 第 7.5 / 9.3 节一致。

---

## 8. API 设计

统一前缀：`/api/v1/orders`  
统一响应：`{ "code", "message", "request_id", "data" }`  
写接口请求头：`Authorization`、`X-Tenant-Id`、`Idempotency-Key`、`X-Request-Id` / `traceparent`

### 8.1 Preview

```http
POST /api/v1/orders/preview
```

```json
{
  "scene": "mall_checkout",
  "channel": "app",
  "coupon_ids": ["coupon_001"],
  "auto_best": true,
  "items": [
    {
      "line_id": "line_1",
      "object_type": "sku",
      "object_id": "sku_1001",
      "quantity": 2,
      "unit_price": 10000,
      "attributes": { "category_id": "category_food", "brand_id": "brand_a" }
    },
    {
      "line_id": "line_2",
      "object_type": "service",
      "object_id": "delivery",
      "quantity": 1,
      "unit_price": 6800
    }
  ],
  "ledger_pay": { "asset_code": "POINT", "amount": 500 }
}
```

响应含 `quote_id`、`original_amount`、`discount_amount`、`payable_amount`、`allocations`、`ledger_pay_amount`、`channel_pay_amount`、`expires_at`。

### 8.2 下单

```http
POST /api/v1/orders
Idempotency-Key: client-ord-20260902-001
```

```json
{
  "client_order_id": "cli_20260902_001",
  "scene": "mall_checkout",
  "channel": "app",
  "quote_id": "quote_001",
  "coupon_ids": ["coupon_001"],
  "items": [],
  "pay_method": "mix",
  "ledger_pay": { "asset_code": "POINT", "amount": 500 },
  "ext": { "store_id": "store_001", "region": "CN-SH" }
}
```

`items` 必须与 quote 快照一致（或省略表示复用 preview 缓存的快照，以服务端保存的 preview 为准）。成功返回 `order_id`、`status=PENDING_PAY`、`reservation_id`、`freeze_id`、`payment_intent`。

### 8.3 查询与动作

```http
GET  /api/v1/orders/{order_id}
GET  /api/v1/orders?status=&scene=&cursor=
POST /api/v1/orders/{order_id}/cancel
POST /api/v1/orders/{order_id}/pay-intent          # 重新拉起支付
POST /api/v1/orders/{order_id}/complete             # 履约完成（内部/适配器）
POST /api/v1/orders/{order_id}/refunds              # 售后
POST /internal/v1/orders/callbacks/payment          # 渠道回告，验签，非公网用户态
```

### 8.4 错误码（与优惠/账本可映射）

| code | 含义 |
|------|------|
| 0 | 成功 |
| 40100 | 未登录 / Token 无效 |
| 40001 | 参数错误、行金额合计不一致 |
| 40910 | 幂等键冲突（参数不同） |
| 40920 | `client_order_id` 已存在且内容不同 |
| 42210 | `QUOTE_STALE` / 过期 / 快照变化 |
| 42211 | 优惠锁定失败（券不可用、预算耗尽等，透传 OfferHub 原因） |
| 42212 | 账本余额不足 / 冻结失败 |
| 42213 | 库存预占失败 |
| 40930 | 状态不允许（如已支付不可取消） |
| 40401 | 订单不存在 |

---

## 9. 数据模型（逻辑）

**orders**

| 字段 | 说明 |
|------|------|
| order_id | 主键，预生成，作 `business_order_id` / 账本 `biz_no` 片段 |
| tenant_id / scene / channel | |
| buyer_user_id | 来自 Auth |
| client_order_id | 调用方幂等键，`unique(tenant_id, buyer_user_id, client_order_id)` |
| status / version | 状态机 + 乐观锁 |
| currency | 与优惠、支付一致 |
| original_amount / discount_amount / payable_amount | 快照 |
| ledger_pay_amount / channel_pay_amount / paid_amount / refunded_amount | |
| quote_id / reservation_id / redemption_id | 可空 |
| freeze_id / asset_code | 可空 |
| pay_method / payment_intent_id | |
| expire_at | 待支付超时 |
| context_json | 传给优惠的 context 快照（store/region 等） |
| created_at / updated_at / paid_at / cancelled_at / completed_at | |

**order_lines**

| 字段 | 说明 |
|------|------|
| line_id | 单内唯一，对齐 OfferHub |
| object_type / object_id | sku / service / entitlement / … |
| quantity / unit_price | 下单快照 |
| original_amount / discount_amount / payable_amount | 行级，含分摊 |
| attributes_json | 已治理字段，报价后不可改 |
| snapshot_json | 标题、图片等展示快照 |

**order_promotions**（可选明细表）

| 字段 | 说明 |
|------|------|
| source_type / source_id | coupon / promotion |
| discount_amount | |
| allocations_json | `[{line_id, discount_amount}]` |
| rule_snapshot_version | 来自报价 |

**order_ledger_legs**

| 字段 | 说明 |
|------|------|
| command | freeze / capture / release / credit |
| biz_no / freeze_id | |
| asset_code / amount | |
| status | accepted / failed |

**order_payments / order_refunds / order_events(outbox)**

支付意图、退款单、Outbox 事件。Outbox 与订单状态变更同一本地事务。

**idempotency_records**

`unique(tenant_id, actor, idempotency_key)`，保存请求哈希与首次响应。

---

## 10. 事件

订单是优惠事件之外的**交易事实源**。建议：

- `order.created.v1`
- `order.paid.v1`
- `order.fulfilled.v1`
- `order.completed.v1`
- `order.cancelled.v1`
- `order.refunded.v1`

信封与 OfferHub 对齐，便于同一套消费与追踪：

```json
{
  "event_id": "evt_001",
  "event_type": "order.paid.v1",
  "event_version": 1,
  "tenant_id": "tenant_001",
  "occurred_at": "2026-09-02T21:00:00+08:00",
  "source": "order-hub",
  "trace_id": "trace_001",
  "data": {
    "order_id": "ord_001",
    "user_id": "u_123",
    "scene": "mall_checkout",
    "payable_amount": 22800,
    "currency": "CNY",
    "redemption_id": "rd_001",
    "freeze_id": "fz_001"
  }
}
```

消费者按 `event_id` 去重。通知中心 / 营销推送只订阅事件，不直连改单。

---

## 11. 一致性、超时与对账

### 11.1 一致性边界

- 订单库本地事务：单据 + 行 + 引用 + Outbox + 幂等记录  
- 优惠锁定、账本冻结：**同步 RPC**，成功才提交本地事务（或先远程后本地，失败补偿）  
- 推荐：**先远程占用，再本地落单**；落单失败靠补偿释放。避免「库里有单但券没锁」的幽灵单。若采用先落单后占用，必须有 `CREATING` 中间态和立即补偿任务。

本方案默认 **先占用后落单**。

### 11.2 超时

| 超时 | 处理 |
|------|------|
| quote TTL（OfferHub 默认约 5m） | Preview 过期须重试算 |
| reservation TTL（约 15m） | 与 `orders.expire_at` 对齐；支付中可 `renew`，次数受限 |
| 订单 `pay_timeout` | Worker 扫描 `PENDING_PAY AND expire_at < now`，走取消 Saga |
| 账本冻结超时 | 以订单关单 Release 为准，不依赖账本自动解冻作为唯一手段 |

### 11.3 对账分层

| 层级 | 对什么 |
|------|--------|
| L1 | 订单 `PAID/COMPLETED` ↔ OfferHub 已核销 `business_order_id` |
| L2 | 有积分/余额的订单 ↔ Ledger `biz_no` 的 Freeze/Capture |
| L3 | `paid_amount` ↔ 支付中台成功金额 |
| L4 | 退款单 ↔ reverse + Credit + 渠道退款 |

差异一律工单 + 补命令，禁止改余额字段或直接改优惠券状态。

---

## 12. 安全与租户

| 项 | 方案 |
|----|------|
| 用户态 | Bearer Token，introspect / JWKS；user_id 不可伪造 |
| 服务态 | 调 OfferHub / Ledger 用 API Key 或 mTLS，与用户 Token 分离 |
| 租户 | 所有查询带 `tenant_id`，禁止跨租户读单 |
| 金额 | 服务端重算行合计，不信任客户端 `payable_amount` |
| 支付回告 | 独立内网路由 + 渠道签名；不暴露为用户 API |
| 审计 | 创建/取消/支付/退款记操作者、前后状态、request_id |

---

## 13. 模块与技术选型建议

与 Ledger Hub / OfferHub 对齐，便于同一团队落地：

| 模块 | 建议 |
|------|------|
| 语言 | Go |
| API | Gin + OpenAPI 3.0 |
| ORM | GORM |
| DB | PostgreSQL（单据、行、Outbox）；大表按时间分区 |
| 缓存 | Redis（Preview 快照、支付中续期锁） |
| 消息 | Kafka / Redpanda；未配置时 Outbox 打日志 |
| 部署 | `cmd/api` 无状态多副本 + `cmd/worker`（超时关单、补偿、对账） |

分层：`iface`（HTTP）→ `application`（Saga）→ `domain`（状态机/金额）→ `infra`（DB/RPC 客户端）。优惠、账本、支付、履约均以接口注入，单测可假实现。

---

## 14. 分阶段交付

### Phase 1 — MVP 闭环

- scene：`mall_checkout`（纯渠道支付）+ `point_mall`（纯账本）
- Preview / Checkout / 查询 / 取消 / 超时关单
- 对接 Auth introspect
- 对接 OfferHub quote + reserve + commit + release
- 对接 Ledger Freeze / Capture / Release（积分场景）
- Outbox 事件、幂等、状态机条件更新

### Phase 2 — 混合支付与售后

- mix：优惠 + 积分 + 渠道
- 支付适配器 + 回调
- 部分退 / 全额退 + reverse + Credit
- reservation renew
- L1/L2 对账报表

### Phase 3 — 多场景与增强

- membership / course / saas 履约适配器
- 订阅续费母子单
- 拆单 / 合单（仍按行 `line_id` 追踪优惠分摊）
- 运营后台单据与补偿工单

---

## 15. 接入样例（商城 + 券 + 积分）

```text
用户 Token
  → Auth introspect = u_123, tenant=mall

Preview
  → OfferHub quote：原价 26800，券减 4000，应付 22800
  → Ledger 余额查询：POINT 可用 800
  → 用户选择抵扣 500 POINT，渠道待付 22300

Checkout  Idempotency-Key=cli_001
  → reserve business_order_id=ord_001
  → Freeze POINT 500  biz_no=order:freeze:ord_001
  → 落单 PENDING_PAY
  → 创建渠道支付 223.00 CNY

支付成功回调
  → PAID
  → commit reservation
  → Capture POINT
  → 履约发货 / 或虚拟发放
  → order.paid.v1
```

取消则 release + Release，渠道关单。

---

## 16. 风险与对策

| 风险 | 对策 |
|------|------|
| 做成「万能交易系统」 | 评审拒绝；库存、定价、渠道清分不进内核 |
| 信任客户端应付价 | 服务端按行重算；折扣只来自 quote |
| 先落单后锁券失败留下脏单 | 默认先占用后落单；若失败补偿任务兜底 |
| 支付回调与超时关单并发 | 状态条件更新；已 PAID 禁止 CANCELLED |
| commit 失败但钱已收 | 停在 PAID + 补偿 Worker，对账 L1 补 commit |
| 优惠与订单金额口径漂移 | 同一最小单位、同一 line_id、quote 哈希 |
| user_id 被 body 篡改 | 只取 Token |

---

## 17. 结论

通用订单系统的通用点不是「一张万能订单表」，而是：

```text
可通用：单据、状态机、金额快照、幂等、Saga 编排、事件、scene 插件
不通用：商品玩法、仓配、渠道合规、优惠规则、资产记账内核
```

它在已有中台中的位置是确定的：

- **用户中心**给 `user_id`  
- **优惠中心**给可核销的报价  
- **账本中台**给可对账的预占与扣减  
- **订单中台**把这三件事做成一笔可关单、可退款、可对账的生意  

推荐落地顺序：**Phase 1 打通「券订单 + 积分订单」两条样板链路 → Phase 2 混合支付与售后 → Phase 3 多 scene 履约。**
